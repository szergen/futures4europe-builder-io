export enum TagCategories {
  person = 'person',
  organization = 'organization',
  project = 'project',
  article = 'article',
  event = 'event',
  domains = 'domains',
  country = 'country',
  // add as many categories as you need
}
